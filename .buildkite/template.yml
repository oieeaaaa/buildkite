# Used by the 'Add to Buildkite' button in the readme
name: "Buildkite Parallel Example"
description: "A parallel example repository you can use as a test project with Buildkite"
steps:
  - command: "buildkite-agent pipeline upload"
    label: ":pipeline:"
